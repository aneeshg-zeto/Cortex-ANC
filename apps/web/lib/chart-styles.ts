/** Recharts tooltip styles that follow light/dark CSS variables */
export const CHART_TOOLTIP_STYLE = {
  background: 'var(--card)',
  border: '1px solid var(--border)',
  color: 'var(--card-foreground)',
  fontSize: 11,
  borderRadius: 8,
} as const;
